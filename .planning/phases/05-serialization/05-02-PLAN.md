---
phase: 05-serialization
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - internal/serialize/view/serializer.go
  - internal/serialize/matview/serializer.go
  - internal/serialize/function/serializer.go
  - internal/serialize/trigger/serializer.go
  - internal/serialize/sequence/serializer.go
  - internal/serialize/compositetype/serializer.go
  - internal/serialize/domain/serializer.go
  - internal/serialize/enum/serializer.go
  - internal/serialize/policy/serializer.go
  - internal/serialize/foreignkey/serializer.go
autonomous: true
requirements:
  - SRLZ-02

must_haves:
  truths:
    - "Every non-table object type produces a correctly structured YAML with kind, schema, name, and type-specific fields"
    - "Each serializer round-trips through Serialize/Deserialize without data loss"
    - "Type assertions match the DDL generator convention for each kind"
  artifacts:
    - path: "internal/serialize/view/serializer.go"
      provides: "View YAML serializer"
      exports: ["Serializer"]
    - path: "internal/serialize/matview/serializer.go"
      provides: "MatView YAML serializer"
      exports: ["Serializer"]
    - path: "internal/serialize/function/serializer.go"
      provides: "Function YAML serializer"
      exports: ["Serializer"]
    - path: "internal/serialize/trigger/serializer.go"
      provides: "Trigger YAML serializer"
      exports: ["Serializer"]
    - path: "internal/serialize/sequence/serializer.go"
      provides: "Sequence YAML serializer"
      exports: ["Serializer"]
    - path: "internal/serialize/compositetype/serializer.go"
      provides: "CompositeType YAML serializer"
      exports: ["Serializer"]
    - path: "internal/serialize/domain/serializer.go"
      provides: "Domain YAML serializer"
      exports: ["Serializer"]
    - path: "internal/serialize/enum/serializer.go"
      provides: "Enum YAML serializer"
      exports: ["Serializer"]
    - path: "internal/serialize/policy/serializer.go"
      provides: "Policy YAML serializer"
      exports: ["Serializer"]
    - path: "internal/serialize/foreignkey/serializer.go"
      provides: "ForeignKey YAML serializer"
      exports: ["Serializer"]
  key_links:
    - from: "internal/serialize/*/serializer.go"
      to: "internal/core/types.go"
      via: "type assertion matching DDL generator convention"
      pattern: "def\\.\\(core\\."
    - from: "internal/serialize/*/serializer.go"
      to: "go.yaml.in/yaml/v3"
      via: "yaml.Marshal/Unmarshal"
      pattern: "yaml\\.(Marshal|Unmarshal)"
---

<objective>
Implement all 10 remaining object serializers (view, matview, function, trigger, sequence, compositetype, domain, enum, policy, foreignkey) following the table serializer pattern from Plan 01.

Purpose: Completes SRLZ-02 -- every object type can be serialized to YAML and deserialized back.
Output: 10 working serializers, all tests green
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-serialization/05-RESEARCH.md
@.planning/phases/05-serialization/05-01-SUMMARY.md

@internal/core/types.go
@internal/core/interfaces.go
@internal/serialize/table/serializer.go

<interfaces>
<!-- Core type definitions for each kind -->

From internal/core/types.go:
```go
// Value type assertions (match DDL generators):
type ViewDef struct {
    ObjectHeader
    Definition string
    Owner      string
}

type MaterializedViewDef struct {
    ObjectHeader
    Definition  string
    Owner       string
    IsPopulated bool
}

type FunctionDef struct {
    ObjectHeader
    Definition string
    ArgTypes   string
    ReturnType string
}

type TriggerDef struct {
    ObjectHeader
    Timing       string
    Events       []string
    TableName    string
    FunctionName string
}

type SequenceDef struct {
    ObjectHeader
    StartValue  int64
    MinValue    int64
    MaxValue    int64
    IncrementBy int64
    Cycle       bool
    Cache       int64
    LastValue   int64
    IsCalled    bool
}

type TypeDef struct {
    ObjectHeader
    Fields []CompositeField
}

type DomainDef struct {
    ObjectHeader
    BaseType        string
    Nullable        bool
    Default         string
    CheckName       string
    CheckDefinition string
}

type EnumDef struct {
    ObjectHeader
    Labels []string
}

type PolicyDef struct {
    ObjectHeader
    TableName string
    Command   string
    Roles     []string
    Using     string
    WithCheck string
}

type ForeignKeyDef struct {
    ObjectHeader
    SourceTable  string
    TargetSchema string
    TargetTable  string
    Definition   string
}
```

Type assertion convention (from DDL generators, Phase 4 decisions):
- View: `def.(core.ViewDef)` (value)
- MatView: `def.(core.MaterializedViewDef)` (value)
- Function: `def.(core.FunctionDef)` (value)
- Trigger: `def.(core.TriggerDef)` (value)
- Sequence: `def.(core.SequenceDef)` (value) -- Phase 4 decision: value receiver
- Type: `def.(core.TypeDef)` (value)
- Domain: `def.(core.DomainDef)` (value)
- Enum: `def.(core.EnumDef)` (value)
- Policy: `def.(core.PolicyDef)` (value)
- ForeignKey: `def.(core.ForeignKeyDef)` (value) -- Phase 4 decision: value receiver
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: View, MatView, Function, Trigger, Sequence serializers</name>
  <files>
    internal/serialize/view/serializer.go,
    internal/serialize/matview/serializer.go,
    internal/serialize/function/serializer.go,
    internal/serialize/trigger/serializer.go,
    internal/serialize/sequence/serializer.go
  </files>
  <behavior>
    - View TestSerialize: ViewDef with definition+owner -> YAML has kind=view, definition, owner
    - MatView TestSerialize: MatViewDef with is_populated=true -> YAML has kind=materialized_view, is_populated=true
    - Function TestSerialize: FunctionDef with arg_types+return_type -> YAML has kind=function, definition, arg_types, return_type
    - Trigger TestSerialize: TriggerDef with timing=BEFORE, events=[INSERT,UPDATE] -> YAML has kind=trigger, timing, events, table_name, function_name
    - Sequence TestSerialize: SequenceDef with all numeric fields -> YAML has kind=sequence, start_value, last_value, is_called etc.
  </behavior>
  <action>
    Replace the stub serializer.go files (created in Plan 01) with full implementations for these 5 kinds. Each serializer follows the exact same pattern as the table serializer from Plan 01:

    1. Define intermediate YAML struct with yaml tags
    2. Implement `toYAML()` conversion from core type to YAML struct
    3. Implement `fromYAML()` conversion from YAML struct back to core type
    4. Implement `Serialize()` with correct type assertion (value assertion for all 5)
    5. Implement `Deserialize()` with unmarshal + fromYAML
    6. Keep `var _ core.Serializer = (*Serializer)(nil)` compile-time check

    YAML field mappings per kind:

    **View:** kind, schema, name, definition, owner
    **MatView:** kind, schema, name, definition, owner, is_populated
    **Function:** kind, schema, name, definition, arg_types, return_type
    **Trigger:** kind, schema, name, timing, events (flow tag), table_name, function_name
    **Sequence:** kind, schema, name, start_value, min_value, max_value, increment_by, cycle, cache, last_value, is_called

    Use `omitempty` sparingly: only on truly optional string fields (like empty defaults). Do NOT use omitempty on bools where false is meaningful (is_populated, cycle, is_called).

    Confirm type assertion pattern by reading `internal/ddl/{kind}/generator.go` for each kind before implementing. Phase 4 decisions say all 5 use value receivers.
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/pgbkrs && go test ./internal/serialize/view/... ./internal/serialize/matview/... ./internal/serialize/function/... ./internal/serialize/trigger/... ./internal/serialize/sequence/... -v -count=1</automated>
  </verify>
  <done>All 5 serializers pass their TestSerialize tests. YAML output has correct kind, field names match yaml tags, type assertions use value receivers. `go test` for all 5 packages PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CompositeType, Domain, Enum, Policy, ForeignKey serializers</name>
  <files>
    internal/serialize/compositetype/serializer.go,
    internal/serialize/domain/serializer.go,
    internal/serialize/enum/serializer.go,
    internal/serialize/policy/serializer.go,
    internal/serialize/foreignkey/serializer.go
  </files>
  <behavior>
    - CompositeType TestSerialize: TypeDef with 2 fields -> YAML has kind=type, fields with name/type
    - Domain TestSerialize: DomainDef with check constraint -> YAML has kind=domain, base_type, nullable, check_name, check_definition
    - Enum TestSerialize: EnumDef with 3 labels -> YAML has kind=enum, labels as flow array
    - Policy TestSerialize: PolicyDef with roles=[PUBLIC], using expression -> YAML has kind=policy, table_name, command, roles, using, with_check
    - ForeignKey TestSerialize: ForeignKeyDef -> YAML has kind=foreign_key, source_table, target_schema, target_table, definition
  </behavior>
  <action>
    Replace the stub serializer.go files for these 5 kinds with full implementations. Same pattern as Task 1.

    YAML field mappings per kind:

    **CompositeType:** kind (="type"), schema, name, fields (array of {name, type})
    **Domain:** kind, schema, name, base_type, nullable (no omitempty), default (omitempty), check_name (omitempty), check_definition (omitempty)
    **Enum:** kind, schema, name, labels (flow tag for inline array)
    **Policy:** kind, schema, name, table_name, command, roles (flow tag), using (omitempty), with_check (omitempty)
    **ForeignKey:** kind (="foreign_key"), schema, name, source_table, target_schema, target_table, definition

    All 5 use value type assertions per Phase 4 convention.

    For Domain: CheckDefinition is passed through verbatim from pg_get_constraintdef (Phase 4 decision). Serialize as-is.
    For Policy: Roles contains "PUBLIC" for polroles={0} (already handled by fetcher).
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/pgbkrs && go test ./internal/serialize/compositetype/... ./internal/serialize/domain/... ./internal/serialize/enum/... ./internal/serialize/policy/... ./internal/serialize/foreignkey/... -v -count=1</automated>
  </verify>
  <done>All 5 serializers pass their TestSerialize tests. YAML output for each kind has correct field names and values. `go test` for all 5 packages PASS. Full suite: `go test ./internal/serialize/... -v` all PASS.</done>
</task>

</tasks>

<verification>
- `go test ./internal/serialize/... -v` -- all 11 serializer packages pass
- `go vet ./internal/serialize/...` -- no issues
- Each serializer satisfies `var _ core.Serializer = (*Serializer)(nil)` compile-time check
</verification>

<success_criteria>
- All 10 non-table serializers produce YAML with correct kind field and type-specific fields
- All 10 serializers implement both Serialize and Deserialize
- Type assertions match DDL generator convention for each kind (all value receivers except table which is in Plan 01)
- `go test ./internal/serialize/... -v` shows all tests PASS
</success_criteria>

<output>
After completion, create `.planning/phases/05-serialization/05-02-SUMMARY.md`
</output>
