---
phase: 09-restore-partial-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/restore/filter.go
  - internal/restore/filter_test.go
  - internal/restore/leak.go
  - internal/restore/leak_test.go
  - internal/restore/logger.go
  - internal/restore/orchestrator.go
  - internal/restore/orchestrator_test.go
  - cmd/pgbackup/cmd/restore.go
autonomous: true
requirements:
  - REST-03
  - REST-10
  - REST-11
  - REST-12
  - REST-13

must_haves:
  truths:
    - "filter.go compiles with buildTransitiveClosure, filteredRestoreOrder, verifyDepsPresent exported and testable"
    - "leak.go compiles with liveObject type, snapshotLiveObjects, detectLeaks"
    - "logger.go compiles with Logger struct, NewLogger, LogDrop, LogRestore, LogLeakWarning, WriteSummary, Close"
    - "orchestrator.go accepts Options struct instead of positional strings — no compile errors in package"
    - "All existing Phase 8 integration tests continue to pass after signature change"
    - "Unit tests for BFS closure and dep verification run green without TEST_DATABASE_URL"
  artifacts:
    - path: "internal/restore/filter.go"
      provides: "BFS transitive closure, filteredRestoreOrder, verifyDepsPresent"
      exports: ["buildTransitiveClosure", "filteredRestoreOrder", "verifyDepsPresent"]
    - path: "internal/restore/filter_test.go"
      provides: "Unit tests for TestBuildTransitiveClosure, TestMissingDependency"
    - path: "internal/restore/leak.go"
      provides: "liveObject type, snapshotLiveObjects, detectLeaks"
    - path: "internal/restore/leak_test.go"
      provides: "Unit test for TestDetectLeaks"
    - path: "internal/restore/logger.go"
      provides: "Logger struct with three log-file writers"
    - path: "internal/restore/orchestrator.go"
      provides: "Updated RunRestore(ctx, conn, Options) signature"
      contains: "type Options struct"
    - path: "cmd/pgbackup/cmd/restore.go"
      provides: "Updated caller constructing Options from flags"
  key_links:
    - from: "cmd/pgbackup/cmd/restore.go"
      to: "internal/restore/orchestrator.go"
      via: "restore.RunRestore(cmd.Context(), conn, opts)"
      pattern: "restore\\.RunRestore"
    - from: "internal/restore/orchestrator.go"
      to: "internal/restore/filter.go"
      via: "filteredRestoreOrder(manifest, opts)"
      pattern: "filteredRestoreOrder"
---

<objective>
Create the Wave 0 infrastructure for Phase 9: three new files (filter.go, leak.go, logger.go), update the orchestrator to use an Options struct, and update all existing callers so the project compiles cleanly. Unit tests for BFS closure and dep verification are written and passing before any integration wiring begins.

Purpose: Nyquist compliance — all subsequent plans build on compilable, unit-tested contracts.
Output: 8 files created or modified; `go build ./...` and `go test ./internal/restore/... -short` pass green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-restore-partial-safety/09-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From internal/resolve/manifest.go:
```go
type Manifest struct {
    Objects      []ObjectEntry  `yaml:"objects"`
    RestoreOrder []RestoreEntry `yaml:"restore_order"`
    // ... metadata fields
}

type ObjectEntry struct {
    ID        string   `yaml:"id"`   // "schema.name" format
    Kind      string   `yaml:"kind"`
    DependsOn []string `yaml:"depends_on"` // []"schema.name" IDs
}

type RestoreEntry struct {
    Schema    string `yaml:"schema"`
    Kind      string `yaml:"kind"`
    Name      string `yaml:"name"`
    FromTable string `yaml:"from_table,omitempty"`
}
```

From internal/restore/orchestrator.go (current signature to replace):
```go
func RunRestore(ctx context.Context, conn *pgx.Conn, backupDir, preBackupDir string) error
```

From cmd/pgbackup/cmd/restore.go (current caller to update):
```go
return restore.RunRestore(cmd.Context(), conn, restoreInputDir, restorePreBackupDir)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: filter.go + filter_test.go — BFS closure, filteredRestoreOrder, verifyDepsPresent</name>
  <files>internal/restore/filter.go, internal/restore/filter_test.go</files>
  <behavior>
    - TestBuildTransitiveClosure_Direct: seed "public.a", which depends_on "public.b"; closure contains both "public.a" and "public.b"
    - TestBuildTransitiveClosure_Transitive: "public.a" depends_on "public.b", "public.b" depends_on "public.c"; closure of ["public.a"] contains all three
    - TestBuildTransitiveClosure_Cycle: graph has "public.a" depends_on "public.b", "public.b" depends_on "public.a"; BFS terminates (visited guard), closure contains both — no infinite loop
    - TestBuildTransitiveClosure_FKEntry: manifest has object with kind "fk" whose DependsOn lists ["public.source", "public.target"]; BFS from "public.source" does NOT follow the FK entry forward (FK objects are not seeds); but post-BFS pass adds FK entries whose full DependsOn set is a subset of the closure
    - TestMissingDependency: verifyDepsPresent returns error containing the missing ID when closure contains an ID not in manifest.Objects; returns nil when all IDs present
    - TestFilteredRestoreOrder_Full: opts with empty Schema and Object returns full manifest.RestoreOrder unchanged
    - TestFilteredRestoreOrder_Schema: opts.Schema="myschema" returns only entries where entry.Schema=="myschema", in original order
    - TestFilteredRestoreOrder_Object: opts.Object="public.a" with transitive deps "public.b" returns both entries in their original manifest order (not re-sorted)
    - TestFilteredRestoreOrder_InvalidObject: opts.Object="notvalid" (no dot) returns error containing "--object must be in schema.name format"
  </behavior>
  <action>
    Create internal/restore/filter.go (package restore):

    ```go
    type Options struct {
        BackupDir    string
        PreBackupDir string
        Schema       string // REST-10: filter to schema (e.g. "myschema")
        Object       string // REST-10: filter to "schema.name"; implies BFS (REST-11)
        LogDir       string // REST-13: parent dir for restore_YYYYMMDD_HHMMSS/ logs
    }
    ```

    Implement buildTransitiveClosure(objects []resolve.ObjectEntry, seeds []string) map[string]bool:
    - Build byID map[string]resolve.ObjectEntry
    - Standard BFS using visited map to prevent cycles
    - After BFS, do a post-BFS pass: for each ObjectEntry with kind=="fk" NOT in visited, if ALL entries in its DependsOn are in visited, add the FK entry's ID to the closure (this ensures FK constraints for restored tables are included per Pitfall 1 in RESEARCH.md)

    Implement verifyDepsPresent(objects []resolve.ObjectEntry, closure map[string]bool) error:
    - Build set of all IDs in manifest.Objects
    - For each ID in closure, if not in the set, return fmt.Errorf("dependency %q is required but not present in backup", id)
    - Return nil if all present

    Implement filteredRestoreOrder(manifest *resolve.Manifest, opts Options) ([]resolve.RestoreEntry, error):
    - If opts.Object != "" and !strings.Contains(opts.Object, ".") → return nil, fmt.Errorf("--object must be in schema.name format, got %q", opts.Object)
    - If opts.Object != "": BFS closure from []string{opts.Object}; call verifyDepsPresent; filter manifest.RestoreOrder keeping entries where schema+"."+name is in closure
    - If opts.Schema != "": filter keeping entries where entry.Schema == opts.Schema
    - Otherwise return manifest.RestoreOrder, nil

    Write filter_test.go (package restore) with the test cases listed in <behavior>.
    Use in-memory resolve.Manifest{} and resolve.ObjectEntry{} structs — no DB required.
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/pgbkrs && go test ./internal/restore/... -run "TestBuildTransitiveClosure|TestMissingDependency|TestFilteredRestoreOrder" -short -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>All filter_test.go tests pass; go vet ./internal/restore/... clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: leak.go + logger.go + orchestrator Options refactor + caller update</name>
  <files>internal/restore/leak.go, internal/restore/leak_test.go, internal/restore/logger.go, internal/restore/orchestrator.go, internal/restore/orchestrator_test.go, cmd/pgbackup/cmd/restore.go</files>
  <behavior>
    - TestDetectLeaks_NoLeaks: preDrop and postDrop are identical sets; detectLeaks returns empty slice
    - TestDetectLeaks_WithLeak: preDrop contains liveObject{Schema:"public",Kind:"table",Name:"orphan"}; postDrop also contains it; shouldBeDropped set includes "public.orphan"; detectLeaks returns ["public.orphan" is a leak warning]
    - TestDetectLeaks_PreExisting: preDrop has "public.preexisting" and postDrop also has it; but "public.preexisting" is NOT in shouldBeDropped; detectLeaks returns empty (not a leak — pre-existing object not in restore_order)
    - All existing Phase 8 tests (TestPreRestoreBackup, TestRestoreCreateOrder, etc.) compile and pass after RunRestore signature change
  </behavior>
  <action>
    1. Create internal/restore/leak.go (package restore):

    ```go
    type liveObject struct {
        Schema string
        Kind   string
        Name   string
    }
    ```

    Implement snapshotLiveObjects(ctx context.Context, conn *pgx.Conn, schemas []string) ([]liveObject, error):
    - Execute the UNION ALL query from RESEARCH.md pattern 5 against pg_catalog
    - Pass schemas as pgx array parameter ($1 = ANY)
    - Scan rows into []liveObject
    - Return all objects found across tables, views, materialized_view, sequences, functions, types, domains, enums

    Implement detectLeaks(postDrop []liveObject, shouldBeDropped map[string]bool) []liveObject:
    - For each obj in postDrop, compute id = obj.Schema+"."+obj.Name
    - If shouldBeDropped[id] is true, append to leaked slice
    - Return leaked (may be empty)

    Create internal/restore/leak_test.go with unit tests for detectLeaks using in-memory data (TestDetectLeaks_NoLeaks, TestDetectLeaks_WithLeak, TestDetectLeaks_PreExisting).

    2. Create internal/restore/logger.go (package restore):

    ```go
    type Logger struct {
        dropLog    *os.File
        restoreLog *os.File
        summaryLog *os.File
        dropCount    int
        restoreCount int
        warnCount    int
        startTime    time.Time
    }
    ```

    Implement NewLogger(logDir string) (*Logger, error):
    - ts := time.Now().Format("20060102_150405")
    - dir = filepath.Join(logDir, "restore_"+ts)
    - os.MkdirAll(dir, 0755)
    - Create drop.log, restore.log, summary.log using os.Create
    - Return populated Logger with startTime = time.Now()

    Implement methods:
    - LogDrop(schema, kind, name string, err error) — writes to dropLog with timestamp, increments dropCount
    - LogRestore(schema, kind, name string, err error) — writes to restoreLog, increments restoreCount
    - LogLeakWarning(schema, kind, name string) — writes to dropLog as a WARN line, increments warnCount
    - WriteSummary(totalDrop, totalRestore, totalWarn int, elapsed time.Duration, restoreErr error) — writes summary to summaryLog
    - Close() error — closes all three files

    Log entry format: `[2006-01-02 15:04:05] DROP  schema=public kind=table  name=mytable → OK`
    On error: replace "OK" with "ERROR: <err>"

    3. Update internal/restore/orchestrator.go:
    - Move the Options struct definition here (it's the orchestrator's primary type)
    - Change RunRestore signature to: func RunRestore(ctx context.Context, conn *pgx.Conn, opts Options) error
    - Replace all uses of backupDir/preBackupDir positional args with opts.BackupDir and opts.PreBackupDir
    - LogDir: if opts.LogDir == "", default to "." (current working dir, matching --pre-backup-dir default)
    - Add stub: at Wave 1 start, call snapshotLiveObjects to get preDrop (skip on error — log to stderr, continue)
    - Add stub: after Wave 1 (DROP), call snapshotLiveObjects to get postDrop; call detectLeaks; for each leak call logger.LogLeakWarning (logger is created at start of RunRestore, closed via defer)
    - Scope: replace hard-coded manifest.RestoreOrder with filteredRestoreOrder(manifest, opts) for all waves
    - The pre-restore backup (Wave 0) is still taken against the FULL DB (not scoped) — this is unchanged
    - Defer logger WriteSummary + Close at end

    4. Update internal/restore/orchestrator_test.go:
    - Change all restore.RunRestore(ctx, conn, backupSubdir, preBackupDir) calls to:
      restore.RunRestore(ctx, conn, restore.Options{BackupDir: backupSubdir, PreBackupDir: preBackupDir})
    - Add stub tests (skip-body with t.Skip("integration only") + t.Log description) for:
      - TestDropLeakDetection (REST-03)
      - TestSchemaScoped (REST-10)
      - TestObjectScopedTransitive (REST-11)
      - TestLogFiles (REST-13)
    - These stubs make the test file compilable for Wave 0; full implementation is in Plan 02

    5. Update cmd/pgbackup/cmd/restore.go:
    - Change RunE to construct Options and call restore.RunRestore(cmd.Context(), conn, restore.Options{BackupDir: restoreInputDir, PreBackupDir: restorePreBackupDir})
    - Add placeholder variables for Schema, Object, LogDir (will be wired to flags in Plan 03)
    - Do NOT add the cobra flag registrations yet (Plan 03 handles that)
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/pgbkrs && go build ./... &amp;&amp; go test ./internal/restore/... -run "TestDetectLeaks" -short -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>go build ./... passes; TestDetectLeaks unit tests pass; all existing orchestrator_test.go tests compile (integration tests skip without TEST_DATABASE_URL)</done>
</task>

</tasks>

<verification>
```bash
cd /home/parallels/workspaces/pgbkrs && go build ./... && go test ./internal/restore/... -short -v 2>&1 | tail -30
```
All unit tests pass. All integration tests skip (not fail) without TEST_DATABASE_URL. No compile errors anywhere.
</verification>

<success_criteria>
- go build ./... exits 0
- go test ./internal/restore/... -short exits 0
- filter_test.go: TestBuildTransitiveClosure*, TestMissingDependency*, TestFilteredRestoreOrder* all green
- leak_test.go: TestDetectLeaks* all green
- Existing Phase 8 integration tests (TestPreRestoreBackup etc.) compile with updated signature
- Options struct defined in orchestrator.go with BackupDir, PreBackupDir, Schema, Object, LogDir fields
</success_criteria>

<output>
After completion, create `.planning/phases/09-restore-partial-safety/09-01-SUMMARY.md`
</output>
